// next-intl.config.ts

const nextIntlConfig = {
  locales: ["en", "mn"],
  defaultLocale: "en",
  requestConfig: "./i18n", // Specify the i18n request config file (no extension)
};

export default nextIntlConfig;
